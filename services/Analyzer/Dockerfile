# Dockerfile for Presidio Analyzer
FROM mcr.microsoft.com/presidio-analyzer:latest

# Expose the API port
EXPOSE 3000

# The container image already includes the necessary entrypoint to start the service
